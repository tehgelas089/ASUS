// document.addEventListener("DOMContentLoaded", () => {
//   const warnaUtama = document.querySelector('meta[name="warna-utama"]')?.content || "{{ session('warna_utama', '#007bff') }}";
//   const warnaSekunder = document.querySelector('meta[name="warna-sekunder"]')?.content || "{{ session('warna_sekunder', '#6c757d') }}";

//   const primary = document.getElementById("primary-element");
//   const secondary = document.getElementById("secondary-element");

//   if (primary) primary.style.backgroundColor = warnaUtama;
//   if (secondary) secondary.style.backgroundColor = warnaSekunder;
// });
